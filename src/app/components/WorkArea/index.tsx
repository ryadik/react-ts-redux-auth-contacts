import * as React from 'react';
import { WorkAreaWrapper } from './style';

const WorkArea: React.FC = () => {
  return <WorkAreaWrapper>work area</WorkAreaWrapper>;
};

export default WorkArea;
